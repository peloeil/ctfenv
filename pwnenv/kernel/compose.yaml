  pwnenv:
    build:
      context: .
      dockerfile: kernel.Dockerfile
    pid: "service:<service-name>"
    cap_add:
      - "SYS_PTRACE"
    depends_on:
      - "<service-name>"
    stdin_open: true
    tty: true
